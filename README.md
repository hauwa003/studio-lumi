# Studio Lumi

Internal OS for Studio Lumi — a single-page static site.

## Development

Open `index.html` directly in a browser, or serve locally:

```bash
npx serve .
```

## Deployment

Deployed to Vercel as a static site.
